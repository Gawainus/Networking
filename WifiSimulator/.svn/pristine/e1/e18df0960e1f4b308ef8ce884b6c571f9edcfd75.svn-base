#ifndef __FTOSTR_H__
#define __FTOSTR_H__


#include <iostream>
#include <vector>
#include <string>
#include <sstream>
#include <string>
#include <iterator>
#include <fstream>
#include <iomanip>
#include <algorithm>

using namespace std;

class Lines
{
	public:
		Lines();
		unsigned int numLines();
		string strAt( unsigned int loc );
		void fToStrs( char * path);

		unsigned int retNumNodes();
		unsigned int retPktLen();
		unsigned int retMax();
		unsigned long long int retDura();
		void retRs(vector<unsigned int> * ranges);

	private:
		vector<string> input; //the class

		unsigned int num;
};

#endif